# TCSystem.Thread.Tests

Tests for `TCSystem.Thread`.

All prerequisites, target frameworks, test commands, repeat-run options, coverage instructions, and test infrastructure
are documented centrally in the repository [unit-test instructions](../../README.md#run-unit-tests).
